# Collatz sequence printed recursively
fn collatz-step {
  as current-n;
  current-n print;
  current-n == 1 if {
    1
  } else {
    current-n % 2 == 0 if {
      current-n / 2 collatz-step
    } else {
      current-n 3 * + 1 collatz-step
    }
  }
}
27 collatz-step;
